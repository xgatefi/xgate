{
  "name": "xgate",
  "version": "0.1.0",
  "private": true,
  "description": "Pay-Per-Tool AI Agent Router with x402 payments",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter gateway dev",
    "build": "pnpm -r build",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "prisma": "^5.7.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

